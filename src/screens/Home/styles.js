import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        margin: 32,
    },
    subtitle: {
        fontSize: 20,
        color: "black",
        fontWeight: "bold",
        marginTop: 40,
        marginBottom: 32,
    }
})

export default styles;